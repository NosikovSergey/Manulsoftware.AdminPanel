# Панель администратора — UX-описание

## Общий каркас

Все страницы после входа имеют единую структуру:
- **Левый сайдбар** — навигация: «Пользователи», «Тарифы»
- **Шапка** — имя текущего администратора, кнопка «Выйти»
- **Хлебные крошки** — навигация назад по иерархии страниц
- **Основная область контента**

Все деструктивные действия (возврат, откат, блокировка, отключение) требуют подтверждения в диалоге. Текст диалога явно описывает что произойдёт.

---

## Страница входа

Центрированная форма на пустом фоне:
- Поле «Логин»
- Поле «Пароль»
- Кнопка «Войти»
- Сообщение об ошибке под формой при неверных данных

---

## Поиск пользователей

`Пользователи`

- Строка поиска вверху (подсказка: «Telegram ID, username или имя»), поиск срабатывает по мере ввода
- Если ничего не введено — таблица пустая, все пользователи сразу не показываются
- Клик на строку → карточка пользователя

**Таблица результатов:**

| Имя | Username | Telegram ID | Баланс | Статус | Дата регистрации |
|---|---|---|---|---|---|
| Иван Петров | @ivanpetrov | 123456789 | 250 ₽ | Активен | 15.01.2024 |

Статус — бейдж: «Активен» (зелёный) / «Заблокирован» (красный).

---

## Карточка пользователя

`Пользователи > Иван Петров`

### Верхний блок (всегда виден вне зависимости от вкладки)

**Левая часть — профиль:**
- Имя и фамилия
- Username (с символом @)
- Telegram ID
- Дата регистрации, последняя активность

**Правая часть — статус и баланс:**
- Бейдж статуса: «Активен» (зелёный) / «Заблокирован» (красный)
- Баланс — крупно; если отрицательный — красным цветом с пометкой «долг»
- Бейдж «Пробный период использован» если флаг выставлен

### Блок действий

Кнопки в ряд:
- «Заблокировать» / «Разблокировать» (зависит от текущего статуса)
- «Выдать права администратора» / «Забрать права администратора»
- «Сбросить пробный период» (неактивна если флаг не выставлен)

### Вкладки

`Ключи | Заказы | Транзакции`

---

#### Вкладка «Ключи»

Клик на строку → карточка ключа.

| Сервер | Статус | Дата истечения |
|---|---|---|
| Netherlands #1 | Активен | 12.06.2026 |

Статус — бейдж: «Активен» (зелёный) / «Отключён» (красный) / «Истёк» (серый).

Статус — вычисляемый из двух полей БД (`IsEnabled` + `ExpiredDate`):
- `IsEnabled = true` и срок не истёк → «Активен»
- `IsEnabled = false` и срок не истёк → «Отключён» (вручную отключён администратором)
- `IsEnabled = false` и срок истёк → «Истёк»

Трафик в таблице не показывается — он доступен только на карточке ключа, так как требует отдельного запроса в Marzban для каждого ключа.

---

#### Вкладка «Заказы»

Клик на строку → карточка заказа.

| Тариф | Статус | Создан | Истекает |
|---|---|---|---|
| 1 месяц | Выдан | 12.05.2026 | 12.06.2026 |

Статус — бейдж с цветом: «Создан» (серый) / «Оплачен» (синий) / «Выдан» (зелёный) / «Истёк» (серый) / «Отменён» (красный) / «Возвращён» (оранжевый).

---

#### Вкладка «Транзакции»

Клик на строку → карточка транзакции.

| Дата | Тип | Сумма | Статус |
|---|---|---|---|
| 12.05.2026 | Пополнение | +299 ₽ | Проведена |
| 13.05.2026 | Списание | −299 ₽ | Проведена |
| 14.05.2026 | Возврат | +299 ₽ | Проведена |

Статусы: «В обработке» / «Проведена» / «Отменена» / «Возвращена».

Визуальный тип транзакции — производный от двух полей БД (`Type` + `Status`):
- Deposit + Committed → «Пополнение»
- Withdrawal + Committed → «Списание»
- Withdrawal + Refunded → «Возврат» (деньги вернули после отмены заказа)
- Deposit + Refunded → «Откат пополнения» (применяется при чарджбэке)

---

## Карточка ключа

`Пользователи > Иван Петров > Ключ #42`

### Основная информация — две колонки

**Левая — данные из нашей базы:**
- Сервер (название + страна)
- Статус: «Активен» / «Отключён» / «Истёк»
- Дата выдачи
- Дата истечения

**Правая — живые данные из Marzban** (с пометкой времени последнего обновления):
- Статус в Marzban: «Active» / «OnHold» / «Expired» / «Limited» (трафик исчерпан)
- Использованный трафик / общий лимит (прогресс-бар: 45 / 300 ГБ)
- Дата истечения в Marzban

Если данные между колонками расходятся — строка подсвечивается жёлтым с предупреждением «Расхождение с Marzban».

### Блок действий

- «Отключить» / «Включить» (зависит от текущего статуса)
- «Продлить бесплатно» → диалог с выбором длительности (1 месяц / 3 месяца / 6 месяцев / 1 год) и подтверждением
- «Восстановить ключ» — активна только при наличии расхождения с Marzban

### Связанный заказ

Ссылка на заказ: тариф, статус, дата истечения. Клик → карточка заказа.

---

## Карточка заказа

`Пользователи > Иван Петров > Заказ #...`

### Основная информация

- ID заказа
- Статус — бейдж с цветом (цвета те же что во вкладке «Заказы»)
- Тариф (длительность)
- Сумма оплаты (берётся из связанной транзакции списания, а не из текущей цены тарифа — цена могла измениться)
- Дата создания
- Дата истечения

### Связи

- Пользователь — ссылка на карточку пользователя
- Ключ — ссылка на карточку ключа (отсутствует если ключ не был выдан)

### Блок действий (зависит от статуса)

**Статус «Создан» или «Оплачен»** (заказ завис, ключ не выдан):
- «Отменить и вернуть деньги»

**Статус «Выдан»** (активная подписка):
- «Вернуть деньги»

**Статус «Истёк» / «Отменён» / «Возвращён»** — действий нет, только просмотр.

Текст диалога подтверждения: «Ключ будет отключён, сумма X ₽ будет возвращена на баланс пользователя».

---

## Карточка транзакции

`Пользователи > Иван Петров > Транзакция #...`

### Основная информация

- ID транзакции
- Дата и время
- Тип: «Пополнение» / «Списание» / «Возврат»
- Сумма
- Статус: «В обработке» / «Проведена» / «Отменена» / «Возвращена»

### Связи

- Пользователь — ссылка на карточку пользователя

### Блок действий (зависит от типа и статуса)

**Тип «Пополнение» + статус «Проведена»:**
- «Откатить транзакцию» — диалог с предупреждением: «Сумма X ₽ будет вычтена из баланса пользователя. Баланс может уйти в минус.»

Все остальные случаи — действий нет, только просмотр.

---

## Управление тарифами

`Тарифы`

Пробный тариф в таблице не отображается.

| Длительность | Цена | Лучший выбор | Статус |
|---|---|---|---|
| 1 месяц | 299 ₽ | — | Включён |
| 3 месяца | 799 ₽ | ✓ | Включён |
| 6 месяцев | 1 499 ₽ | — | Выключен |
| 1 год | 2 699 ₽ | — | Включён |

- **Статус** — toggle прямо в таблице, изменение применяется мгновенно
- **Лучший выбор** — чекбокс прямо в строке, изменение применяется мгновенно
- **Цена** — кликабельное поле, при нажатии становится редактируемым инлайн; рядом появляется кнопка «Сохранить»

---

## Смена пароля

Доступна через шапку: имя администратора → «Сменить пароль».

`Сменить пароль`

- Поле «Текущий пароль»
- Поле «Новый пароль»
- Поле «Повторите новый пароль»
- Кнопка «Сохранить»
- Сообщение об ошибке если текущий пароль неверен или пароли не совпадают
- Сессия после смены пароля остаётся активной
